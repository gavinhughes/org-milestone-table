#+title: Backlog
#+strartup: overview

* fix: C-c C-c: Leave point in place
* Minimum required column names are
- ID
- Pred / Predecessor
- Date

Error if not present.

* Predecessor column name can be either Predecessor or Pred
* fix: CI linting


* fix: org-milestone-table-add-missing-ids should start filling any unused numbers beginning with 1
Currently starts at highest number.
* fix: Enable critical path highlighting on multiple tables in same buffer
Current implementation: adding highlighting on a second table, removes highlighting from first table
* Feature: Superfluous predecessor indication
Consider:

| ID | Pred |
|----+------|
|  1 |      |
|  2 | 1    |
|  3 | 1,2  |

Predecessor 1 on ID 3 is superfluous and receives a visual indication:

| ID | Pred |
|----+------|
|  1 |      |
|  2 | 1    |
|  3 | ~1~,2  |

Configurable:
org-milestone-superfluous-predecessor-???

Issues a warning on:
org-milestone-table-update-timeline

* Feature: Export timeline to simple Gantt-style ASCII chart
* Feature: Interactive milestone insertion with completion
* Feature: Integration with Org agenda views
* Maybe
** Feature: Support for business-day offsets (skip weekends)
* [X] Done
** DONE fix: add-missing-ids: on undo, leave point in place
*** commit da4a653
=fix: add-missing-ids restores point on undo=

Pushed the pre-command point onto =buffer-undo-list= inside a
=with-undo-amalgamate= group, so a single =undo= reverts every inserted ID
and leaves point exactly where the user invoked the command.

** DONE Feature: Highlight critical path in timeline
** DONE change: org-milestone-table-new omits Status column
*** commit b41eec2
=change: org-milestone-table-new omits Status column=

New tables now contain only the four columns the package uses
(=ID=, =Pred=, =Date=, =Milestone=). Status is not read or written by
any package function and should not appear in the template.

** DONE fix: Item having same date as predecessor should sort beneath predecessor
** DONE Feature: Year unit (=y=) support in Pred offsets
** DONE Feature: Improved error and warning message presentation
Have them pop up in another buffer of a type that has best UX.
*** commit a48f199
=feat: display validation errors in a dedicated buffer=

Errors are collected during =omt--resolve= and displayed in a
=*Milestone Table Errors*= buffer. The buffer is dismissed on the next
successful C-c C-c run.

** DONE Feature: IDs of the form "15?" indicate that predecessor(s) currently unknown
- Leave undated but when sorting the timeline, keep adjacent to ID 15, or other 15?
*** commits cd6c646, 6a69835
=Add fuzzy milestone ID support (NNN?)=
=fix: exclude fuzzy IDs from circular reference detection=

Fuzzy IDs (e.g. =15?=) are recognised, left undated, sorted immediately
before their base row, and excluded from cycle detection.

** DONE Feature: Error on circular predecessor reference
*** commit (cycle detection shipped as part of =omt--resolve=)
Cycle detection uses a =visited= accumulator in =omt--resolve= (line 180).
Fuzzy IDs are explicitly excluded from the check.
** DONE Feature: Negative month offsets (e.g. =1-2m=)
** DONE Multiple predecessors
- Format required: Separated by commas
- The longest predecessor is applied

** DONE Feature: Sort rows by date
- New command: ~org-milestone-table-sort-by-date~
- Sorts data rows ascending by Date column; dateless rows go to end.

** DONE Column label can be "Pred" or "Predecessors"

** DONE Add missing IDs
- New command: ~org-milestone-table-add-missing-ids~
- Finds max existing numeric ID, assigns incrementing IDs to rows without one.

** DONE Bugfix: cl-return-from without cl-block
- Wrapped ~omt--resolve~ body in ~cl-block~ so cycle detection works.


Implement outstanding features.
~[send]~



Here's the diff for the Code block:

#+begin_src diff
--- a/code.el
+++ b/code.el
@@ -1,6 +1,22 @@
+(defun gh/org-milestone-table-dwim ()
+  "Run all milestone table operations in sequence: add IDs, update dates, sort."
+  (interactive)
+  (unless (org-at-table-p)
+    (user-error "Not in a table"))
+  (org-milestone-table-add-missing-ids)
+  (org-milestone-table-update-timeline)
+  (org-milestone-table-sort-by-date))
+
+(defun gh/org-milestone-table-cc-hook ()
+  "Hook for `org-ctrl-c-ctrl-c-hook' that runs milestone table DWIM."
+  (when (and (org-at-table-p)
+             (save-excursion
+               (goto-char (org-table-begin))
+               (looking-at "^[ \t]*|.*\\bID\\b.*\\bPred\\b\\|\\bPredecessors\\b")))
+    (gh/org-milestone-table-dwim)
+    t))
+
+(add-hook 'org-ctrl-c-ctrl-c-hook #'gh/org-milestone-table-cc-hook)
+
 (defun org-milestone-table-empty ()
   "Insert an empty milestone table at point."
   (interactive)
#+end_src

And here's the full updated file:

#+TITLE: Org Milestone Table
#+STARTUP: overview
–––

- LLM Instructions
- Each time you edit code, reproduce all headings in this file with all content updated.

- Example Table

| ID | Pred|       Date | Milestone                   |
|----+-------+------------+-----------------------------|
|  4 |       | 2026-10-22 | ~Award~                       |
|  7 | 4     | 2026-10-22 | Send DAR letter to DND      |
|  1 |       | 2027-10-01 | ~ECC Issued~|
|  2 | 1+3m  | 2028-01-01 | PPA Permit                  |
|  3 | 1+1m  | 2027-11-01 | Fire Safety Certification   |
|  6 | 1,2,3 | 2028-01-01 | ~Start Construction~          |
|  5 |       |            | Business ("Mayor's") Permit |

- Changelog
** DONE Feature: C-c C-c runs full milestone table pipeline
- ~gh/org-milestone-table-dwim~ calls add-missing-ids, update-timeline, sort-by-date in sequence.
- ~gh/org-milestone-table-cc-hook~ added to ~org-ctrl-c-ctrl-c-hook~; detects milestone tables by header pattern.
